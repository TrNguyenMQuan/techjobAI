[pytest]
# Thêm thư mục gốc vào sys.path để `import source.*` và `import pipeline.*` hoạt động
pythonpath = .
# asyncio_mode=auto: không cần @pytest.mark.asyncio trên từng async test
asyncio_mode = auto
