# ─── Core ────────────────────────────────────────────────────
pyspark==3.5.8          # Spark engine (kèm JVM binaries ~300MB)
python-dotenv==1.0.1    # load .env vào os.environ

# ─── HTTP crawl ──────────────────────────────────────────────
httpx[http2]==0.27.0    # async HTTP client, hỗ trợ HTTP/2 (nhanh hơn requests cho concurrent)
tenacity==8.5.0         # decorator retry với exponential backoff

# ─── Dev / verify ────────────────────────────────────────────
# duckdb==1.0.0 — removed: requires C compiler to build wheel on this machine
#                 dùng Spark DataFrame.show() hoặc psql để verify data thay thế

# S3
boto3==1.34.14
psycopg[binary]>=3.2

# ─── dbt (Transformation layer) ──────────────────────────────
dbt-core==1.8.9
dbt-postgres==1.8.0

# ─── ML / Embedding ──────────────────────────────────────────
sentence-transformers==3.0.1
sqlalchemy>=1.4.0,<2.0.0
psycopg2-binary>=2.9.0

# ─── Web Backend ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.28.0
