name: 'techjob_ai'
version: '1.0.0'

profile: 'techjob_ai'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  techjob_ai:
    staging:
      +schema: staging
      +materialized: view
    warehouse:
      +schema: warehouse
      +materialized: table
    aggregates:
      +schema: warehouse
      +materialized: table
